# ⚛️ React Props  

---

## 📖 What are Props?  
**Props** (short for **properties**) are used to **pass data** from a **parent component** to a **child component**.  

- Think of props as **function arguments** in React.  
- Props are **read-only** → child components **cannot modify** them.  

---

## 🏗 Using Props in Functional Components  

```jsx
function Welcome(props) {
  return <h1>Hello, {props.name}! 👋</h1>;
}

function App() {
  return (
    <div>
      <Welcome name="Alice" />
      <Welcome name="Bob" />
    </div>
  );
}
```

✅ Here, `name` is passed as a **prop** from `App` → `Welcome`.  

---

## 🎨 Destructuring Props  

Instead of writing `props.name`, you can **destructure** props directly.  

```jsx
function Welcome({ name }) {
  return <h1>Hello, {name}! 🎉</h1>;
}
```

---

## 🧩 Passing Multiple Props  

```jsx
function Profile({ name, age, role }) {
  return (
    <div>
      <h2>{name}</h2>
      <p>Age: {age}</p>
      <p>Role: {role}</p>
    </div>
  );
}

function App() {
  return (
    <div>
      <Profile name="Alice" age={25} role="Frontend Developer" />
      <Profile name="Bob" age={30} role="Backend Developer" />
    </div>
  );
}
```

---

## 🔁 Props are Immutable  

Props **cannot be changed** by child components.  If you need data that changes → use **state** instead.  

❌ Wrong way (modifying props):
```jsx
function Welcome(props) {
  props.name = "Changed"; // ❌ Not allowed
  return <h1>{props.name}</h1>;
}
```

✅ Correct way: Use **state** for mutable values.  

---

## 🌍 Real-World Example: Button with Props  

```jsx
function Button({ label, color }) {
  return (
    <button style={{ backgroundColor: color, padding: "10px", margin: "5px" }}>
      {label}
    </button>
  );
}

function App() {
  return (
    <div>
      <Button label="Login" color="green" />
      <Button label="Register" color="blue" />
    </div>
  );
}
```

---

## ✅ Summary  

| Concept              | Description |
|----------------------|-------------|
| Props                | Pass data into components |
| Read-only            | Cannot be modified by child components |
| Destructuring Props  | Cleaner way to access props |
| Multiple Props       | Pass multiple values to components |

---

## 🧠 Best Practices  

* Always keep props **read-only**.  
* Use **destructuring** for cleaner code.  
* Use **prop-types** (or TypeScript) for validating props.  
* Pass only **necessary props** to keep components clean.  
